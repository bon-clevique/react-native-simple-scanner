# SimpleScannerExample - クイックスタートガイド

## 🚀 5分でできる！実機テスト

### 前提条件

- ✅ iPhoneがUSBで接続されている
- ✅ Xcodeがインストールされている
- ✅ Apple Developer アカウント（無料でOK）

---

## ステップ1: Xcodeで開く（1分）

```bash
cd example/ios
open SimpleScannerExample.xcworkspace
```

---

## ステップ2: 署名設定（2分）

1. **プロジェクトナビゲーター（左側）** で `SimpleScannerExample` をクリック
2. **TARGETS > SimpleScannerExample** を選択
3. **Signing & Capabilities** タブを開く
4. **✓ Automatically manage signing** をチェック
5. **Team** で自分のアカウントを選択
6. **Bundle Identifier** を変更（例: `com.yourname.scanner`）

---

## ステップ3: ビルド・実行（2分）

1. **デバイス選択**（上部ツールバー）で接続したiPhoneを選択
2. **▶ Run** ボタンをクリック（または `Cmd + R`）
3. 初回のみ：iPhoneで **設定 > 一般 > プロファイル** から開発元を信頼

---

## ✅ テスト

1. **カメラ権限を許可**
2. **QRコードをスキャン**
3. **フラッシュボタン**（右上）をタップして動作確認

---

## 💡 トラブルシューティング

### ビルドエラーが出たら

```bash
# クリーンビルド
Xcode > Product > Clean Build Folder (Shift + Cmd + K)
```

### カメラが映らない

- 設定 > SimpleScannerExample > カメラ で権限を確認

### Metro Bundlerエラー

```bash
cd example
npm start -- --reset-cache
```

---

## 📖 詳細な手順

詳しい説明は `DEVICE_TESTING.md` を参照してください。
