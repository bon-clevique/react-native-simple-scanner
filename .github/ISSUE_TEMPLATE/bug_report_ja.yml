name: 🐛 バグ報告（日本語）
description: 再現可能なバグやリグレッションを報告してください。
labels: [bug]
body:
  - type: markdown
    attributes:
      value: |
        # バグ報告

        👋 こんにちは！

        **新しいIssueを作成する前に、以下の項目を必ずご確認ください ❗**
        *(必要な情報が不足している場合、Issueがクローズされる可能性があります)*

  - type: checkboxes
    attributes:
      label: 送信前の確認
      description: 基本的な確認を行ってください。
      options:
        - label: 最新バージョンのライブラリでテストしました（バグが既に修正されている可能性があります）。
          required: true
        - label: サポートされているReact Nativeバージョンでテストしました。
          required: true
        - label: 既存のIssueを検索し、重複がないことを確認しました。
          required: true

  - type: textarea
    id: summary
    attributes:
      label: バグの概要
      description: |
        バグの内容を明確かつ簡潔に説明してください。
        必要に応じて、エラーメッセージ、スタックトレース、スクリーンショット、GIFなども添付してください。
    validations:
      required: true

  - type: input
    id: library-version
    attributes:
      label: ライブラリのバージョン
      description: 使用しているライブラリのバージョンは何ですか？
      placeholder: 'x.x.x'
    validations:
      required: true

  - type: textarea
    id: react-native-info
    attributes:
      label: 環境情報
      description: ターミナルで `npx react-native info` を実行し、結果をここに貼り付けてください。
      render: shell
    validations:
      required: true

  - type: textarea
    id: steps-to-reproduce
    attributes:
      label: 再現手順
      description: |
        問題を再現するための手順とコードを明確に記載してください。
      value: |
        1. …
        2. …
    validations:
      required: true

  - type: input
    id: reproducible-example
    attributes:
      label: 再現可能なリポジトリ
      description: 再現可能なサンプルを含むGitHubリポジトリのリンクを提供してください。
    validations:
      required: true
